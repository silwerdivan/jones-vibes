You are an autonomous coding agent.
Your Goal: Complete the tasks in `2. cyberpunk-designer-overhaul-phase-[x].md`.

Context:
1. Read `2. cyberpunk-designer-overhaul-phase-[x]-activity.md` to see what happened last.
2. Read `2. cyberpunk-designer-overhaul-phase-[x].md` to pick the next unchecked task.

Instructions:
1. Pick the SINGLE highest priority unchecked task.
2. Implement it.
3. Run verification (e.g., `npm run build`). Ensure the code is syntactically correct and compiles.
4. If verified, mark [x] in `2. cyberpunk-designer-overhaul-phase-[x].md` and append a summary to `2. cyberpunk-designer-overhaul-phase-[x]-activity.md`.
5. **CRITICAL:** Complete ONLY ONE task at a time. STOP and wait for user instruction before starting the next task.
6. Once ALL tasks in `2. cyberpunk-designer-overhaul-phase-[x].md` are checked:
   - Run ALL tests (`npm test`).
   - If tests pass, generate a brief summary of the completed phase.
   - Extract any key "lessons learnt" to steer future direction.
   - Append both the summary and the lessons learnt to the respective sections at the bottom of `1. cyberpunk-designer-overhaul.md`.
   - Commit all changes for the phase (e.g., `git add . ; git commit -m "Phase [x] completed: [Summary]"`).
   - Rename `2. cyberpunk-designer-overhaul-phase-[x].md` to `2. cyberpunk-designer-overhaul-phase-[x]-completed.md`.

Constraints:
- Do NOT output conversational text.
- Do NOT commit changes until the entire phase is complete and verified with tests.