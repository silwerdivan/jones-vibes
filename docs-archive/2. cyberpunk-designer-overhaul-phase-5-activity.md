# Phase 5 Revision: Mobile UX Corrections

## Summary
The initial Phase 5 execution failed to meet the required UX standards for the HUD, Life Screen, and Labor Sector navigation. This revision focuses on correcting these specific regressions while maintaining the mobile-first goals.

## Tasks

### 1. HUD Time-Ring Clarity (Reversion & Fix)
- [x] Revert the time-ring to the "old" style (colored circular border directly around the avatar).
- [x] Add a dark semi-transparent backdrop or high-contrast drop shadow *behind* the colored ring to ensure visibility against complex backgrounds (e.g., `profit-line-goes-up.png`).
- [x] Ensure the text inside (e.g., "4h") remains highly readable with a distinct shadow.
- **Files:** `style.css`, `src/ui/components/HUD.ts`

### 2. Life Screen Layout Fix
- [x] Fix the stretched gauge cards on mobile (as seen in Screen 2).
- [x] Implement a 2-column grid layout for gauges on mobile (`grid-template-columns: 1fr 1fr; gap: 10px;`) or a more compact vertical stack.
- [x] Update `.gauge-card` and `.gauge-svg-container` CSS to ensure the circular gauges scale properly without being pushed off-center or dwarfed by empty space.
- [x] Align labels and numeric values within the gauge cards for better clarity.
- **Files:** `style.css`, `src/ui/components/screens/LifeScreen.ts`

### 3. Labor Sector & Modal Navigation (Close Icon & "Work Shift" Fix)
- [x] Remove the massive pink "WORK SHIFT" footer button from the Labor Sector dashboard.
- [x] Integrate the "Work Shift" action as a more reasonable, standard-sized button within the "Career Protocols" section (e.g., as a primary action card or a button next to the current job).
- [x] Implement a persistent "Close" icon (X) in the top-right corner of the Location Modal header.
- [x] Ensure the "Close" icon successfully replaces the "Leave Location" footer button across all location dashboards.
- **Files:** `src/ui/UIManager.ts`, `src/ui/components/Modal.ts`, `style.css`

## Validation Checklist
- [x] HUD time-ring is visually appealing and clear against the "Profit Line" background.
- [x] Life Screen gauges are not stretched and look correct on mobile view.
- [x] Labor Sector has no giant footer button.
- [x] Location Modal has a working "Close" (X) icon in the top-right corner.
- [x] No regressions in the Onboarding flow or Weekend Summary styling.
- [x] Build succeeds (TSC and Vite).
