# Phase 4 Activity Log

## Step 1: Real Insolvency Implementation
Implemented debt mechanics to enforce Burn Rate. Missed payments now result in debt, interest, and service penalties.

### Files Modified:
- `src/models/types.ts`: Added `debt` to `PlayerState` and `CAR` to `RandomEventEffect` and `Requirement`.
- `src/game/Player.ts`: Added `debt` property and methods `addDebt`, `payDebt`, and `getWorkEfficiency`.
- `src/EventBus.ts`: Added `DEBT_CHANGED`, `CAR_PURCHASED`, and `BANK_PAY_DEBT` events.
- `src/systems/TimeSystem.ts`: Updated `endTurn` to handle partial Burn Rate payments, apply 10% debt interest, and trigger `SUBSCRIPTION_DEFAULT`.
- `src/ui/components/HUD.ts`: Added `debt` display to the HUD.
- `src/ui/components/screens/LifeScreen.ts`: Added `debt` to financial overview and status chips.
- `src/systems/EconomySystem.ts`: Added `payDebt` method and updated `buyCar` to publish `CAR_PURCHASED`.
- `src/ui/UIManager.ts`: Added "Pay Debt" action to the Cred-Debt Ctr dashboard.
- `src/main.ts`: Wired `BANK_PAY_DEBT` event.
- `src/data/conditions.ts`: Added `SUBSCRIPTION_DEFAULT` condition.

## Step 2: Labor and Upgrade Math Correction
Ensured `workShift()` correctly applies `WAGE_MULTIPLIER` and `WORK_EFFICIENCY` modifiers.

### Files Modified:
- `src/game/GameState.ts`: Updated `workShift()` payout calculation.
- `src/game/Player.ts`: Ensured `wageMultiplier` getter is correct.

## Step 3: Hustle Rebalance
Nerfed `Donate Blood-Plasma` to ensure stable labor is more efficient for credits/hour.

### Files Modified:
- `src/data/hustles.ts`: Reduced reward and increased time/sanity cost for `Donate Blood-Plasma`.

## Step 4: Event Deck Integrity & Car Support
Fixed unreachable event gates and added car-specific risks.

### Files Modified:
- `src/data/randomEvents.ts`: 
    - Fixed `Transit Strike`, `local_bank_glitch`, and `local_fastfood_glitch`.
    - Added `Speeding Ticket` and `Car Accident` events.
- `src/game/EventManager.ts`: 
    - Updated `evaluatePrerequisites` for `hasCar`.
    - Updated `triggerEvent` for `CAR` requirement.
    - Updated `applyEffect` for `CAR` effect.

## Step 5: Regression and Verification
Updated and added tests to ensure all new mechanics work as intended and old ones don't break.

### Files Modified:
- `tests/TimeSystem.test.ts`: Updated tests for debt and sanity.
- `tests/HypnoScreen.test.ts`: Updated tests for sanity.
- `tests/EventManager.test.ts`: Updated for car-gated choices and location mapping.
- `tests/GameState.test.ts`: Added tests for labor modifiers.
- `tests/HustleBalance.test.ts`: (New file) Added to verify hustle vs job efficiency.

### Verification Results:
- `npm test`: All 290 tests passed.
- `npm run test:e2e`: All 4 tests passed.
- Grep checks: No 'Slums', 'Credit Union', or `id: 'Car'` in requirements remain in `src/`.
