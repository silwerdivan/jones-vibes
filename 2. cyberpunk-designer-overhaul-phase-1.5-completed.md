# Phase 1.5: Cyberware & Terminology Cleanup

## Overview
Address the "leftovers" from Phase 1: scrub legacy "Omni Creds" terminology and populate the Ripperdoc Clinic with initial content.

## Tasks

- [x] **Step 1: Terminology Scrub**
  - **Task:** Perform a global search for "Omni Creds", "Omnicreds", "Cash", and "$" to ensure the transition to **Credits (₡)** is 100% complete across all UI, data, and tests.
  - **Affected Files (Core Logic):**
    - [x] `src/models/types.ts`: Rename `cash` -> `credits`, `CASH` effect -> `CREDITS`, `cashChange` -> `creditsChange`.
    - [x] `src/game/Player.ts`: Rename `cash` -> `credits`, `addCash` -> `addCredits`, `spendCash` -> `spendCredits`.
    - [x] `src/systems/EconomySystem.ts`: Update all `cash` logic, `_formatMoney` (ensure ₡ symbol), and event publishing.
    - [x] `src/systems/TimeSystem.ts`: Update `_formatMoney`, `cashChange`, and `Omni-Chill` references.
    - [x] `src/game/AIController.ts`: Update all `player.cash` checks to `player.credits`.
    - [x] `src/EventBus.ts`: Rename `STATE_EVENTS.CASH_CHANGED` to `STATE_EVENTS.CREDITS_CHANGED`.
  - **Affected Files (Data & Events):**
    - [x] `src/data/items.ts`: Review items for legacy "Omni" branding (e.g., `Omni-Chill`).
    - [x] `src/data/randomEvents.ts`: Update `type: 'CASH'` -> `type: 'CREDITS'`, and scrub flavor text of "Omni-Creds" or "eddies".
  - **Affected Files (UI):**
    - [x] `src/ui/components/HUD.ts`: Update `data-cash` attribute -> `data-credits`, and `updateCash` -> `updateCredits`.
    - [x] `src/ui/components/screens/CityScreen.ts`: Update `locationHint` for "Omni-Creds" (Labor Sector).
    - [x] `src/ui/ClockVisualization.ts`: Check for any hardcoded currency strings.
  - **Affected Files (Tests):**
    - [x] `tests/AIController.test.ts`
    - [x] `tests/EducationSystem.test.ts`
    - [x] `tests/EventManager.test.ts`
    - [x] `tests/GameState.test.ts`
    - [x] `tests/OmniChill.test.ts` (Renamed to `tests/ThermalRegulator.test.ts`)
    - [x] `tests/PersistenceStress.test.ts`
    - [x] `tests/state-events.test.ts`
    - [x] `tests/TimeSystem.test.ts`
    - [x] `tests/services/PersistenceService.test.ts`
    - [x] `tests/ui/components/HUD.test.ts`
    - [x] `tests/ui/components/screens/CityScreen.test.ts`
    - [x] `tests/ui/components/shared/ActionCard.test.ts`

- [x] **Step 2: Ripperdoc Content Injection**
  - **Task:** Populate the Ripperdoc Clinic with initial Cyberware inventory so it isn't empty.
  - **Affected Files:**
    - [x] `src/data/items.ts`: Add at least 1 more distinct Cyberware item (currently has 2) to reach the required 3 items.
    - [x] `src/ui/components/screens/CityScreen.ts`: Verify `handleLocationClick` correctly opens the dashboard for `Ripperdoc Clinic`.

- [x] **Step 3: UI/UX Pass**
  - **Task:** Check the EULA modal and any other "Flavor Text" files for legacy currency/stat names.
  - **Affected Files:**
    - [x] `src/data/eula.ts`: Review for "Omni-Creds" or similar.
    - [x] `src/ui/components/EulaModal.ts`: Verify rendering of currency terms.

## Validation
- **Grep Check (Terminology):** 
  - `grep -riE "omni|cash|\\$" src/` (Should return 0 results for legacy currency context, excluding necessary system/regex uses).
  - `grep -ri "omnicred" src/` (Should return 0 results).
- **Grep Check (Events):**
  - `grep -r "CASH_CHANGED" src/` (Should return 0 results).
- **Functional Check:** Open the Ripperdoc Clinic in the UI and verify 3 items are purchasable and functional.
- **Unit Tests:** All updated tests must pass (`npm test`).
